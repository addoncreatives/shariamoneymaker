name: Run LLM Council Agent

on:
  schedule:
    # Kører hver dag klokken 03:00 UTC (hvilket svarer til klokken 04:00 i GMT+1)
    - cron: '0 3 * * *'
  workflow_dispatch: # Giver mulighed for at starte scriptet manuelt fra GitHub-brugerfladen

jobs:
  run-analyst:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

    - name: Run LLM Council Script
      env:
        EMAIL_SENDER: ${{ secrets.EMAIL_SENDER }}
        EMAIL_PASSWORD: ${{ secrets.EMAIL_PASSWORD }}
        EMAIL_RECEIVER: ${{ secrets.EMAIL_RECEIVER }}
      run: |
        python llm_council.py
